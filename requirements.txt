Django==5.1.2
mysqlclient==2.2.4
asgiref==3.8.1
sqlparse==0.5.1
pytz==2024.1

# If you are using crispy forms
django-crispy-forms==2.1

# If using dotenv for secret keys
python-dotenv==1.0.1

# For handling user sessions, auth hashing already inside Django
# No need to install separately

# OPTIONAL (only if used in your project)
Pillow==10.4.0   # Images handling (profile photo etc.)
djangorestframework==3.15.2  # REST APIs
